/**
 * List of application route names used for navigation and routing.
 *
 * @constant
 * @type {string[]}
 *
 * @example
 * // Example usage
 * import { routesNames } from "./routes";
 *
 * if (routesNames.includes("login")) {
 *   console.log("Login route exists!");
 * }
 *
 * @description
 * Available routes:
 * - "home": Home page
 * - "about": About page
 * - "taskList": Task list view
 * - "login": Login page
 * - "register": Registration page
 * - "reset-password": Password reset page
 * - "profile": User profile page
 * - "sitemap": Sitemap view
 */

export const routesNames = ["home", "about", "taskList", "login", "register", "reset-password", "profile", "sitemap"]